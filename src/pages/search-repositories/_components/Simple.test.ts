// A very simple test file to check basic Vitest functionality

it('should be true', () => {
  expect(true).toBe(true);
});

describe('Another simple suite', () => {
  it('1 + 1 should be 2', () => {
    expect(1 + 1).toBe(2);
  });
});
